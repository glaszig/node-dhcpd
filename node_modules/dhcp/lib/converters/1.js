/**
 * option 1: subnet mask
 */
var sprintf = require('../sprintf'),
    utils = require('../utils'),
    decode = utils.readIp,
    encode = utils.writeIp;

module.exports = {
  "encode": encode, "decode": decode
}